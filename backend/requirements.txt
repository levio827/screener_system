# ============================================================================
# Stock Screening Platform - Backend Dependencies
# ============================================================================

# Web Framework
fastapi==0.121.3  # Security: Latest stable, supports starlette 0.49.1
starlette==0.50.0  # Security: Fixed HTTP Range DoS (GHSA-7f5h-v6xp-fcq8, CVE-2025-62727)
uvicorn[standard]==0.38.0
gunicorn==23.0.0  # Security: Fixed request smuggling (GHSA-w3h3-4rj7-4ph4, GHSA-hc5x-x2vx-497g)

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.30.0
alembic==1.17.1
psycopg2-binary==2.9.11

# Cache
redis[hiredis]==5.0.1

# Authentication & Security
python-jose[cryptography]==3.4.0  # Security: Fixed algorithm confusion and JWT bomb (PYSEC-2024-232, PYSEC-2024-233)
bcrypt==5.0.0
python-multipart==0.0.20  # Security: Fixed ReDoS vulnerabilities (GHSA-2jv5-9r88-3w3p, GHSA-59g5-xgcq-4qw3)
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# Background Tasks
# Note: Celery 5.4.0+ supports redis 5.0+
celery[redis]==5.4.0

# Data Processing
pandas==2.1.3
numpy==1.26.2

# HTTP Requests
httpx==0.25.2
aiohttp==3.13.2  # Security: Fixed request smuggling in chunked trailer parsing (GHSA-9548-qrrj-x5pj, CVE-2025-53643)

# Monitoring & Logging
prometheus-client==0.23.1
sentry-sdk[fastapi]==2.45.0  # Security: Fixed env variable exposure (GHSA-g92j-qhmh-64v2)

# Testing
pytest==9.0.1
pytest-asyncio==1.3.0
pytest-cov==4.1.0
aiosqlite==0.21.0
freezegun==1.5.1
# httpx already listed in HTTP Requests section

# Code Quality
black==25.11.0  # Security: Fixed ReDoS (PYSEC-2024-48)
flake8==7.3.0
mypy==1.18.2
isort==7.0.0

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
