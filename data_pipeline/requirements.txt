# Apache Airflow and Providers
# =============================================================================

# Core Airflow with constraints for Python 3.11
# Airflow 3.1.2 fixes werkzeug and flask-appbuilder vulnerabilities
apache-airflow[postgres,celery]==3.1.2  # Security: Latest stable, fixes werkzeug & flask-appbuilder CVEs

# Database Drivers
# =============================================================================
asyncpg==0.30.0  # Async PostgreSQL driver
psycopg2-binary==2.9.11  # PostgreSQL driver (for Airflow metadata DB)

# Data Processing
# =============================================================================
pandas==2.1.4
numpy==1.26.2

# HTTP Client
# =============================================================================
requests==2.32.5
httpx==0.25.2  # Async HTTP client

# Cache
# =============================================================================
redis==7.1.0  # Redis client for caching

# Utilities
# =============================================================================
python-dotenv==1.0.0  # Environment variable management

# Testing
# =============================================================================
pytest==9.0.1
pytest-asyncio==0.21.1
